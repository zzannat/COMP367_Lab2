This is my pipeline script pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Build the Maven project
                echo 'mvn clean install'
            }
        }
    }
}
